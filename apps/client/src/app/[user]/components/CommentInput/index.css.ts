import { style } from "@vanilla-extract/css";

export const commentFormStyle = style({
  width: "100%",
});

export const leaveCommentWrapper = style({
  display: "flex",
  alignItems: "center",

  padding: "0.8rem 1.2rem 0.8rem 2rem",
});

export const commentInputStyle = style({
  marginLeft: "1.2rem",
});
